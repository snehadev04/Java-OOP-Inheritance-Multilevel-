// Define the 'power' class that extends 'advcal' to add power operation
public class power extends advcal 
{
    // Method to calculate the power of one number raised to another
    double po(double n1, double n2) 
    {
        return Math.pow(n1, n2);
    }
}
